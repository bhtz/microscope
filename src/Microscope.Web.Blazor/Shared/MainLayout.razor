@inherits LayoutComponentBase
@using MudBlazor.Services

@inject IBreakpointService BreakpointListener

<MudThemeProvider Theme="currentTheme" />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <Header DarkMode="DarkMode" DrawerToggle="DrawerToggle" />
    <MudDrawer @bind-Open="_drawerOpen" Elevation="25" ClipMode="DrawerClipMode.Never" Variant="@DrawerConfig" MiniWidth="60px">
        <NavMenu />
    </MudDrawer>
    <MudMainContent>
        <MudContainer MaxWidth="MaxWidth.False" Class="mt-4">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>